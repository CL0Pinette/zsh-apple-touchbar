#!/bin/zsh

# Like top on statusline

# CPU usage: top -l $num | grep "CPU usage"
# CPU temp: sudo powermetrics --samplers smc
#
# GPU usage: ???
# GPU temp: sudo powermetrics --samplers smc
#
# Mem usage: vm_stats + awk script
# Swap Mem Usage:
#
# Have to update all perdiocally every n seconds
